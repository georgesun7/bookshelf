import PageLayout from "@/components/PageLayout";
import VideoSection from "@/components/VideoSection";

const Video = () => {
  return (
    <PageLayout title="video work">
      <VideoSection />
    </PageLayout>
  );
};

export default Video;
